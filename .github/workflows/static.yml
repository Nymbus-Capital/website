# This workflow is disabled - replaced by deploy.yml
name: Static Deploy (Disabled)

on:
  workflow_dispatch:
    inputs:
      placeholder:
        description: 'This workflow is disabled'
        required: false

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use deploy.yml instead."
